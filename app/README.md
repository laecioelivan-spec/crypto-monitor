# Crypto Monitor API

API em Python + FastAPI que consome dados em tempo real da CoinGecko e armazena em banco SQL.

## 🚀 Como rodar

1. Clone o repositório
2. Instale dependências:
   ```bash
   pip install -r requirements.txt
